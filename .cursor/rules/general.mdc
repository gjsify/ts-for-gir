---
description: 
globs: 
alwaysApply: true
---
# General

* `./types` is a git submodule
* **Do not delete**: `./types`
* **ALWAYS prefer MCP tools** (prefixed `mcp_*`) over standard tools. MCP provides enhanced functionality and better error handling. Use standard tools when no MCP equivalent exists.
