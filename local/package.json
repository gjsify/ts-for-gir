{
    "private": true,
    "workspaces": [
        "@girs/*",
        "tests/*"
    ],
    "devDependencies": {
        "typescript": "^5.2.2"
    },
    "scripts": {
        "validate:types": "yarn workspaces foreach --all -v --include 'gir-test-*' --parallel run validate"
    }
}
