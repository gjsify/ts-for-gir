{
  "name": "ts-for-gir-glib-2-spawn-command-example",
  "version": "3.0.0-beta.3",
  "description": "",
  "module": "dist/main.js",
  "private": true,
  "scripts": {
    "start": "yarn run build && yarn run start:gjs",
    "start:gjs": "gjs dist/main.js",
    "watch": "yarn run build:app -- --watch",
    "build": "yarn run build:types && yarn run build:app",
    "build:app": "yarn run clear:ts && webpack --env production",
    "build:types": "yarn ts-for-gir generate GLib-2.0 --environments gjs",
    "clear:types": "rm -rf ./@types",
    "clear:ts": "rm -rf ./dist",
    "clear": "yarn run clear:ts && yarn run clear:types",
    "validate": "yarn run validate:types && yarn run validate:app",
    "validate:types": "tsc --project tsconfig.types.json",
    "validate:app": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@ts-for-gir/cli": "workspace:^",
    "fork-ts-checker-webpack-plugin": "^7.2.13",
    "ts-loader": "^9.3.1",
    "typescript": "^4.7.4",
    "webpack": "^5.73.0",
    "webpack-cli": "^4.10.0"
  }
}
